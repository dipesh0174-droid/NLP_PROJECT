 version: '3.8'

services:
  web:
    build: 
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ../data:/app/data
      - ../logs:/app/logs
    environment:
      - FLASK_ENV=production
      - DEBUG=False
    restart: unless-stopped
