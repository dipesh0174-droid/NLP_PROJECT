app:
  name: "Hindi Sentiment Analyzer"
  version: "1.0.0"

nlp:
  languages: ['hi', 'ta', 'te']
  model_name: "ai4bharat/indic-bert"
  max_length: 128

paths:
  models: "data/models"
  cache: "data/cache"
  logs: "logs"

server:
  host: "0.0.0.0"
  port: 5000
